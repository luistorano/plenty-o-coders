<div class="divider"></div>
<div class="section">
    <div class="row">
        <div class="col s12 m6 post">
            {{#if post}}
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <a href="{{post_url}}" target="_blank" class="card-title">{{title}}</a>
                    <h5>{{vote_count}} {{format_plural "point" vote_count}} by {{user.username}} on {{format_date created_at}}</h5>
                    <p>{{question}}</p>
                </div>
                <div class="card-action">
                    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
                </div>
                {{/if}}
            </div>
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <span class="card-title">Card Title</span>
                    <p>I am a very simple card. I am good at containing small bits of
                        information. I am convenient because I require little markup to use
                        effectively.</p>
                </div>
                <div class="card-action">
                    <a href="#">This is a link</a>
                    <a href="#">This is a link</a>
                </div>
            </div>
        </div>
    </div>
</div>